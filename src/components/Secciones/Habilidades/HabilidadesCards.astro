---
import { vIconsFile } from '../../../../public/js/index.js';
import JSONHabilidades from "../../../../public/js/JSONHabilidades.json";
---

{JSONHabilidades.map((item) => {
  return (
    <div class="cHabilidadCard">
      <div class="cHabiIcono">
        <svg aria-hidden="true">
          <use href={`${vIconsFile}${item.jsonIcono}`}></use>
        </svg>
      </div>
      <h3 class="cHabiNombre">{item.jsonArea}</h3>
      <div class="cHabiDetalles">
        {item.jsonSoftwares.map((itemLine) => {
          return <p>{itemLine}</p>;
        })}
      </div>

    </div>
  );
})}

<style>

  .cHabilidadCard {
    
    border: 5px solid  var(--vHabColorHR1);
    background-color: var(--vHabColorHR2);
    color: var(--vHabColorHR1);
    box-shadow: var(--vHabColorHR3, 1) 0px 0px 3px 3px;
    border-radius: 2rem;
    padding: 1%;
    display: flex;
    flex-direction: column;
    align-items: center;
    --vWidthCard: 20rem;
    width: var(--vWidthCard);
    height: calc(var(--vWidthCard) + 2rem);
    @media ( width <= 500px ) {
      width: calc(var(--vWidthCard) - .5rem);
      height: calc(var(--vWidthCard) + 1rem);
    }
    @media ( width <= 400px ) {
      width: calc(var(--vWidthCard) - 2.5rem);
      height: calc(var(--vWidthCard) + 1rem);
    }

    & .cHabiIcono {
      height: 33%;
      display: flex;
      align-items: center;

      & svg {
        --vSise: 4rem;
        width: var(--vSise);
        height: var(--vSise);
        fill: var(--vHabColorHR1);
        stroke: var(--vHabColorHR1);
        stroke-width: 2.5;
      }
    }
    & .cHabiNombre {
      height: 33%;
      font-family: var(--vAardvarkCafe);
      font-weight: 500;
      --vFontSize: 32px;
      font-size: var(--vFontSize);
      text-shadow: 1px 1px #FFFFFF;
      display: flex;
      align-items: center;
      @media ( width <= 400px ) {
        font-size: calc(var(--vFontSize) - 2px);
      }
    }
    & .cHabiDetalles {
      width: 100%;
      height: 33%;
      text-align: center;
      font-family: var(--vFrutiger);
      font-size: 20px;
      font-weight: bold;
      padding: 0 1rem;
      line-height: 1.5rem;
      @media ( width <= 500px ) {
        font-size: 18px;
      }
      @media ( width <= 400px ) {
        font-size: 16px;
      }
    }
  }

</style>