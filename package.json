{
  "name": "astro-landaetadev",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "deploy": "DEPLOY_ENV=GH_PAGES npm run build && gh-pages -d dist -b gh-pages -t",
    "astro": "astro"
  },
  "dependencies": {
    "astro": "^6.0.8",
    "gh-pages": "^6.3.0",
    "vite": "^8.0.1"
  }
}
