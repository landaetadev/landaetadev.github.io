import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  site: 'https://www.landaetadev.com', // tu dominio personalizado
  base: '/', // la raíz del dominio
});